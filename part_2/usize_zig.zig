const std = @import("std");

// const Orang = struct {
//     nama: []const u8,
//     umur: u32,
// };

pub fn main() void {
    // const buah = [_][]const u8{ "apel", "rambutan", "mangga", "jeruk", "semangka" };
    //
    // for (buah, 0..) |buah_baru, index| {
    //     std.debug.print("buah ke {d}: {s}\n", .{ index, buah_baru });
    // }
    // const ukuran = @sizeOf(Orang);
    // std.debug.print("ukuran dari struct Orang adalah: {d} byte\n", .{ukuran});
    // const panjang_string: usize = "wello apa kabar".len;
    // std.debug.print("ukuran dari string len adalah: {d}\n", .{panjang_string});
    // const angka_besar: usize = 500;
    // const ubah_ke_angka_kecil: u8 = @intCast(angka_besar);
    // std.debug.print("hasil ubah ke angka kecil adlah: {d}\n", .{ubah_ke_angka_kecil});

    // error
    // var nilai_x: usize = 10;
    // nilai_x -= 15;
    // std.debug.print("nilai x sekarang adalah: {d}\n", .{nilai_x});
}
